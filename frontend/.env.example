# Backend API URL — only needed for production (Vercel deployment)
# Leave empty for local dev (Vite proxy handles it automatically)
# Set this in your Vercel Frontend project → Settings → Environment Variables
VITE_API_URL=https://your-healthgpt-backend.vercel.app
